package constants

const (
	AND string = "$and"
	OR  string = "$or"
	NOT string = "$not"
	NOR string = "$nor"

	EQ    string = "$eq"
	NE    string = "$ne"
	LIKE  string = "$like"
	ILIKE string = "$ilike"
	GT    string = "$gt"
	GTE   string = "$gte"
	LT    string = "$lt"
	LTE   string = "$lte"
	NIN   string = "$nin"
	IN    string = "$in"
)
